# 参赛信息展示模块实现计划

## 1. 执行摘要

作为 CTO，我为您设计了**参赛信息展示模块**的实现方案。该模块旨在以一种**优雅且高效**的方式，在应用内引导用户为我们的项目投票和阅读相关文章，从而提高项目的曝光度和社区参与度。

**核心思路**：我们将在应用的**侧边栏底部**创建一个独立的、可复用的组件，用于展示投票和文章链接。这个组件将是**非侵入式**的，既能吸引用户注意，又不会干扰核心用户流程。

---

## 2. UI/UX 设计

我们将在 `components/layout/Sidebar.tsx` 中添加一个新的组件 `CompetitionInfo.tsx`。该组件将包含：

*   一个醒目的标题，如“支持我们！”或“为我们投票”。
*   一个指向 Snapshot 投票页面的**主要按钮**，文案为“为「证道」投票”。
*   两个指向 X.com 文章的**次要链接**，文案分别为“了解我们的理念”和“查看项目更新”。

**设计原则**：
*   **简洁明了**：避免过多文字，直接展示核心操作。
*   **视觉吸引**：按钮和链接将使用项目的主题色，并带有合适的图标。
*   **响应式设计**：在移动端视图下，该模块将自动调整布局，确保信息清晰可见。

---

## 3. 任务分配

由于这是一个独立的 UI 任务，我建议将其分配给一位擅长前端开发的 AI 员工。

| 任务编号 | 任务名称 | 负责员工 | 预估工时 | 任务描述 |
| :--- | :--- | :--- | :---: | :--- |
| **TASK-COMP-01** | **参赛信息展示模块开发** | **AI #3 (UI 开发)** | 3 小时 | 创建并集成 `CompetitionInfo` 组件到侧边栏。 |

### 详细任务指令

#### 👉 发送给 AI 员工 #3（UI 开发）

```text
你是 AI #3（UI 开发）。

【任务】开发并集成参赛信息展示模块。

【操作步骤】
1. 拉取最新代码：git clone https://github.com/Jasoncong111/zhengdao 或 git pull origin main
2. 创建新组件：`components/CompetitionInfo.tsx`
3. 在该组件中，实现以下 UI 元素：
   - 一个标题：“为我们投票！”
   - 一个主要按钮，链接到：https://snapshot.box/#/s:bnbdev-dao.eth/proposal/0xb48bf6be495602d66663a78fb95db9f22b405680ca896a8597b68356b5645416
   - 两个次要链接，分别链接到：
     * https://x.com/blockphd7/status/2016875033600413996?s=20
     * https://x.com/blockphd7/status/2015605299844194809?s=20
4. 将 `CompetitionInfo` 组件集成到 `components/layout/Sidebar.tsx` 的底部。
5. 确保所有链接在新标签页中打开（`target="_blank"`）。

【完成后】提交代码并告知。
```

---

## 4. 验收标准

1.  应用的侧边栏底部出现“为我们投票！”模块。
2.  点击投票按钮和文章链接后，能够正确跳转到指定的 URL。
3.  在移动端视图下，该模块显示正常。

我将会在任务完成后，进行最终的技术验收。
