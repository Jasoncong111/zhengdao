# 体验模式（Demo Mode）实现计划（V2 - 修订版）

## 1. 执行摘要

根据您的提醒和我的核查，我更新了**体验模式（Demo Mode）**的实现计划。`TASK-DEMO-01`（虚拟数据后端）已经由一位表现出色的 AI 员工提前超额完成。因此，我已将计划修订，以反映当前的代码库状态，确保任务分配的精确性。

**核心思路**：我们将直接利用 `lib/demo-data.ts` 中已完成的、功能强大的虚拟数据函数，重点将放在前端的集成工作上，确保游客模式能够无缝调用这些预设数据，提供高保真的产品体验。

---

## 2. 虚拟数据系统设计（已完成）

`lib/demo-data.ts` 文件已经实现了一个非常完善的虚拟数据系统，其功能完全满足甚至超越了原计划的要求。关键功能包括：

*   **丰富的预设数据**：包含 `demoUserProfile`, `demoLifeGoals`, `demoReflections` 等，为游客提供了立即可见的丰富内容。
*   **动态复盘数据生成**：`generateDemoReviewData(period: number)` 函数能够根据请求的时长（如7天）精确生成分析报告，完美解决了数据范围准确性的问题。
*   **动态个人主页数据**：`generateDemoProfileData(newCheckIn?: Reflection)` 函数能够将用户在体验模式下的新打卡与预设历史结合，动态生成一个真实的个人主页视图。

**结论**：虚拟数据后端的任务已经圆满完成，无需重复开发。

---

## 3. 任务分配（修订版）

基于上述情况，我对任务进行了重新分配：

| 任务编号 | 任务名称 | 负责员工 | 预估工时 | 状态 | 任务描述 |
| :--- | :--- | :--- | :---: | :--- | :--- |
| **TASK-DEMO-01** | **虚拟数据后端实现** | AI #1 | 4 小时 | ✅ **已完成** | `lib/demo-data.ts` 已包含所有必需功能。 |
| **TASK-DEMO-02** | **前端组件集成** | **AI #2 (全栈)** | 5 小时 | ⏳ **待办** | 修改前端页面，调用 `lib/demo-data.ts` 中的函数。 |
| **TASK-DEMO-03** | **“即将推出”页面** | **AI #3 (UI)** | 2 小时 | ⏳ **待办** | 创建一个静态页面，展示未来规划的功能。 |

### 详细任务指令（修订版）

#### 👉 发送给 AI 员工 #2（全栈开发）

```text
你是 AI #2（全栈开发）。

【任务】集成体验模式的前端数据调用逻辑。

【背景】虚拟数据后端已由其他同事在 `lib/demo-data.ts` 中实现。你的任务是利用这些现成的函数来填充前端页面。

【操作步骤】
1.  拉取最新代码：`git pull origin main`
2.  仔细阅读 `lib/demo-data.ts` 文件，熟悉其中可用的函数，特别是 `generateDemoProfileData` 和 `generateDemoReviewData`。
3.  修改个人主页 (`/app/profile/page.tsx`) 和复盘页面 (`/app/review/[period]/page.tsx`)。
4.  在这些页面中加入判断逻辑：当用户处于未连接钱包的“体验模式”时，数据源应从真实的 API 请求切换为调用 `lib/demo-data.ts` 中的虚拟数据函数。
    *   对于个人主页，使用 `generateDemoProfileData` 来获取数据。
    *   对于复盘页面，根据 `period` 参数调用 `generateDemoReviewData(period)` 来获取数据。
5.  确保在个人主页上，用户能看到自己刚刚提交的体验打卡记录和预设的打卡历史。

【完成后】提交代码并告知。
```

#### 👉 发送给 AI 员工 #3（UI 开发）

```text
你是 AI #3（UI 开发）。

【任务】创建一个“即将推出”的静态展示页面。

【操作步骤】
1.  拉取最新代码：`git pull origin main`
2.  创建一个新的路由页面，例如 `/app/coming-soon/page.tsx`。
3.  在该页面中，设计一个简洁的 UI，用于展示未来规划的功能列表（例如：社交功能、多链支持、AI 教练等）。
4.  在主导航或相关位置添加入口链接。

【完成后】提交代码并告知。
```

---

## 4. 验收标准

1.  未连接钱包的用户点击“体验模式”后，可以正常进入应用。
2.  用户可以输入人生规划和进行一次打卡。
3.  用户的个人主页能正确显示通过 `generateDemoProfileData` 生成的虚拟数据。
4.  7 天复盘页面能正确显示通过 `generateDemoReviewData(7)` 生成的虚拟分析数据。
5.  “即将推出”页面可以正常访问和展示。

我将会在所有任务完成后，进行最终的技术验收。
