# 体验模式（Demo Mode）实现计划

## 1. 执行摘要

作为 CTO，我为您制定了**体验模式（Demo Mode）**的完整实现计划。该计划旨在为未连接钱包的用户提供一个**高保真**的产品演示流程，让他们能够无障碍地体验“证道”的核心价值，从而提高用户转化率。

**核心思路**：我们将创建一个**虚拟数据层**，在用户进入“体验模式”时，系统将从该层读取预设的、真实的模拟数据，并结合用户在体验过程中产生的**单次打卡数据**，动态生成一个完整的个人主页和复盘报告。

---

## 2. 虚拟数据系统设计

为了让体验尽可能真实，我们需要设计一套能够以假乱真的虚拟数据。

### A. 数据结构

我们将创建一个 `lib/demo-data.ts` 文件，用于存放和生成所有虚拟数据。数据结构如下：

```typescript
// 虚拟用户配置
export const demoUserProfile = {
  name: "体验用户",
  avatar: "/images/avatars/default.png",
  level: 3, // 当前等级
  sbtLevel: 2, // 已铸造的 SBT 等级
};

// 预设的打卡历史（6天）
export const demoCheckInHistory = [
  { date: '2026-01-28', content: '完成了冥想训练，感觉内心平静了许多。', mood: '平静' },
  { date: '2026-01-27', content: '阅读了《原则》的前三章，收获很大。', mood: '充实' },
  // ... 更多数据
];

// 预设的人生规划
export const demoLifeGoals = [
  { goal: '在今年内学会用 Rust 开发一个简单的 DApp。', status: '进行中' },
  { goal: '每周坚持健身 3 次，保持身体健康。', status: '进行中' },
];
```

### B. 动态数据生成

*   **个人主页数据**：当用户在体验模式下完成一次打卡后，我们将用户的**新打卡记录**与 `demoCheckInHistory` 合并，形成一个包含 7 条记录的完整列表，并展示在虚拟个人主页上。
*   **复盘数据**：我们将创建一个 `generateDemoReviewData(period: number)` 函数。当用户请求 7 天复盘时，该函数将精确地从合并后的 7 条打卡记录中生成分析报告，**确保不会出现超过 7 天的数据**。

---

## 3. 任务分配

我已将该项目分解为以下三个独立的任务，可以由三位 AI 员工并行执行：

| 任务编号 | 任务名称 | 负责员工 | 预估工时 | 任务描述 |
| :--- | :--- | :--- | :---: | :--- |
| **TASK-DEMO-01** | **虚拟数据后端实现** | **AI #1 (后端专家)** | 4 小时 | 创建 `lib/demo-data.ts`，实现所有虚拟数据的生成逻辑。 |
| **TASK-DEMO-02** | **前端组件集成** | **AI #2 (全栈开发)** | 6 小时 | 修改个人主页和复盘页面，使其在体验模式下调用虚拟数据。 |
| **TASK-DEMO-03** | **“即将推出”页面** | **AI #3 (UI 开发)** | 2 小时 | 创建一个静态页面，展示未来规划的功能。 |

### 详细任务指令

#### 👉 发送给 AI 员工 #1（后端专家）

```text
你是 AI #1（后端专家）。

【任务】实现体验模式的虚拟数据后端。

【操作步骤】
1. 拉取最新代码：git clone https://github.com/Jasoncong111/zhengdao
2. 创建新文件：`lib/demo-data.ts`
3. 在该文件中，实现以下功能：
   - 定义并导出 `demoUserProfile`, `demoCheckInHistory` (至少6条), `demoLifeGoals` (至少2条) 的数据结构和内容。
   - 创建并导出 `generateDemoReviewData(period: number)` 函数，该函数能根据请求的时长（如 7 天），精确地生成对应天数的复盘分析数据，确保数据范围准确无误。
   - 创建并导出 `getMergedCheckIns(newCheckIn)` 函数，该函数能将用户在体验模式下的新打卡记录与预设历史合并。

【完成后】提交代码并告知。
```

#### 👉 发送给 AI 员工 #2（全栈开发）

```text
你是 AI #2（全栈开发）。

【任务】集成体验模式的前端数据调用逻辑。

【操作步骤】
1. 拉取最新代码：git clone https://github.com/Jasoncong111/zhengdao
2. 修改个人主页 (`/app/profile/page.tsx`) 和复盘页面 (`/app/review/[period]/page.tsx`) 。
3. 在这些页面中加入判断逻辑：当用户处于未连接钱包的“体验模式”时，数据源应从真实的 API 请求切换为调用 `lib/demo-data.ts` 中的虚拟数据函数。
4. 确保在个人主页上，用户能看到自己刚刚提交的体验打卡记录和预设的打卡历史。

【完成后】提交代码并告知。
```

#### 👉 发送给 AI 员工 #3（UI 开发）

```text
你是 AI #3（UI 开发）。

【任务】创建一个“即将推出”的静态展示页面。

【操作步骤】
1. 拉取最新代码：git clone https://github.com/Jasoncong111/zhengdao
2. 创建一个新的路由页面，例如 `/app/coming-soon/page.tsx`。
3. 在该页面中，设计一个简洁的 UI，用于展示未来规划的功能列表（例如：社交功能、多链支持、AI 教练等）。
4. 在主导航或相关位置添加入口链接。

【完成后】提交代码并告知。
```

---

## 4. 验收标准

1.  未连接钱包的用户点击“体验模式”后，可以正常进入应用。
2.  用户可以输入人生规划和进行一次打卡。
3.  用户的个人主页能正确显示包含新打卡记录在内的虚拟数据。
4.  7 天复盘页面能正确显示 7 天的虚拟分析数据。
5.  “即将推出”页面可以正常访问和展示。

我将会在所有任务完成后，进行最终的技术验收。
